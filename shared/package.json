{
  "name": "@universal-crawler/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared types and utilities for Universal Crawler",
  "main": "types/index.ts",
  "types": "types/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "echo \"No tests in shared workspace\" && exit 0",
    "lint": "echo \"No linting in shared workspace\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
